# Base URL of the backend API (must include the /api prefix).
# Local development (backend running on your machine):
VITE_API_URL=http://localhost:8080/api

# Production (deployed backend on Railway) — used automatically by `vite build`
# via .env.production:
# VITE_API_URL=https://maintainiq-backend-production.up.railway.app/api
